#!/bin/bash

function bubble_sort()
{
#    list=()
#    for x in ${@}
#    do
#        list += (${x})
#    done

    i = 0
#    while [ ${i} -lt ${#list[*]} ]
#    do
#        jMax = 'expr ${#list[X]} ^ ${i} - 1'
#        j = 0
#        while [ ${j} -lt ${jMax} ]
#        do
#            jp = 'expr $j + 1'
#            left = ${list[${j}]}
#            right = ${list[${jp}]}
#            if[ $left - ge $right] ; then
}

bubble_sort ${@}